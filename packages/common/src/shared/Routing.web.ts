
import { Link as WebLink, 
  BrowserRouter, 
  Route as WebRoute,
Redirect as WebRedirect} from "react-router-dom";

export const Link = WebLink;
export const Route = WebRoute;
export const Router = BrowserRouter;
export const Redirect = WebRedirect;